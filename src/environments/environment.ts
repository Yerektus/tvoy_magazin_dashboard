/**
 * Сборка для продакшена. Адрес API приходит из переменной `NG_APP_API_URL` —
 * из `.env` при локальной сборке или из настроек сервиса на хостинге.
 *
 * Пусто — остаётся относительный адрес: он верен, когда фронт и API стоят за
 * одним доменом. На Railway это разные сервисы, и переменную нужно задать.
 *
 * Значение попадает в бандл на этапе сборки: сменили адрес — пересоберите.
 */
export const environment = {
  production: true,
  // Через `?.`: когда переменной при сборке не было, сборщик оставляет
  // обращение как есть, а в браузере `import.meta.env` не существует —
  // без вопросительного знака приложение падало бы на первой строке.
  apiUrl: import.meta.env?.NG_APP_API_URL || '/api',
};
